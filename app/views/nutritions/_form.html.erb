<%= form_with(model: nutrition, local: true) do |form| %>
  <% if nutrition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nutrition.errors.count, "error") %> prohibited this nutrition from being saved:</h2>

      <ul>
      <% nutrition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :nutrition_name %>
  </div>

  <div class="field">
    <%= form.label :calorie %>
    <%= form.text_field :calorie, id: :nutrition_calorie %>
  </div>

  <div class="field">
    <%= form.label :fat %>
    <%= form.text_field :fat, id: :nutrition_fat %>
  </div>

  <div class="field">
    <%= form.label :food_name %>
    <%= form.text_field :food_name, id: :nutrition_food_name %>
  </div>

  <div class="field">
    <%= form.label :cusine %>
    <%= form.text_field :cusine, id: :nutrition_cusine %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
